import React from 'react';
import Layout from '../components/Layout';
import Hero from '../components/Hero';

const Home: React.FC = () => (
  <Layout>
    <Hero />
  </Layout>
);

export default Home;
